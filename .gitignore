node_modules/
python/venv/
python/downloads/
python/separated/
.env
*.log
.DS_Store
Thumbs.db

# POS security (never commit)
.pos_key
data/pos.db
data/backups/

# License secrets (never commit)
data/.license_secret
data/.admin_secret
data/.master_admin
data/licenses.json
subscriptions.json

# Credentials (never commit)
docs/.credentials
*.credentials
*.db
*.db-shm
*.db-wal
.claude/worktrees/
.android-sdk/
.android-sdk-tmp/

# Release signing (NEVER commit — lose this = can't update app on Play Store)
.keystore/
*.jks
*.keystore
keystore.properties
