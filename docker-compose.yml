version: "3.8"

services:
  byflow:
    build: .
    container_name: byflow
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    environment:
      - PORT=8080
      - NODE_ENV=production
